{
  "name": "meds-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:data": "node scripts/generate-data.mjs",
    "validate:data": "node scripts/generate-data.mjs --validate",
    "validate:assets": "node scripts/validate-assets.mjs",
    "dev:data": "node scripts/generate-data.mjs --dev-assets",
    "dev": "astro dev --host 127.0.0.1",
    "check": "astro check",
    "prebuild": "node scripts/generate-data.mjs && node scripts/validate-assets.mjs",
    "build": "astro build && node scripts/copy-documents.mjs",
    "preview": "astro preview --host 127.0.0.1"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.4",
    "astro": "^5.15.6",
    "gray-matter": "^4.0.3",
    "typescript": "^5.9.3"
  },
  "devDependencies": {}
}
