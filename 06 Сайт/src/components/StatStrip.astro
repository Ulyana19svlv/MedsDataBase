---
const { stats } = Astro.props;
const items = [
  ["Люди", stats.people],
  ["События", stats.events],
  ["Документы", stats.documents],
  ["Показатели", stats.metrics || 0],
];
---

<dl class="stat-strip">
  {items.map(([label, value]) => (
    <div>
      <dt>{label}</dt>
      <dd>{value}</dd>
    </div>
  ))}
</dl>
